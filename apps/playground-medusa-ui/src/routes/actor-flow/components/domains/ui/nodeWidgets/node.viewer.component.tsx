import { Button, Avatar, Tag } from '@shopify/polaris';
const ComponentMapping: any = {
  Button: Button,
  Avatar: Avatar,
  Tag: Tag,
};

export const NodeViewerComponent = ({ data }: any) => {
  // const { model, temperature, maxTokens, systemPrompt } = data;
  return <div>NodeViewerComponent</div>;
};
